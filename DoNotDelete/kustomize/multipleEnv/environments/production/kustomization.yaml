resources:
  - ../../application
namePrefix: production-
configMapGenerator:
- name: example-config
  namespace: default
  #behavior: replace
  files:
    - configs/config.json
patchesStrategicMerge:
  - env.yaml
