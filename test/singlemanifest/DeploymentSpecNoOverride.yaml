apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-single-file-deployment
  labels:
    app: nginx
spec:
  replicas: 0
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: k8s-single-file-deployment
        image: library/nginx:1.18
        ports:
        - containerPort: 80
